// src/App.jsx
import React from "react";
import { motion } from "framer-motion";

const SectionTitle = ({ children }) => (
  <h2 className="text-3xl md:text-4xl font-bold text-center mb-6 bg-clip-text text-transparent bg-gradient-to-r from-indigo-400 via-pink-500 to-purple-400">
    {children}
  </h2>
);

const scrollTo = (id) => {
  const el = document.getElementById(id);
  if (el) el.scrollIntoView({ behavior: "smooth", block: "start" });
};

export default function App() {
  return (
    <div className="relative min-h-screen bg-gradient-to-b from-black via-slate-950 to-black text-white font-inter">
      {/* Floating glows */}
      <motion.div
        aria-hidden
        className="pointer-events-none fixed -left-20 -top-10 w-80 h-80 rounded-full bg-gradient-to-br from-indigo-600 to-pink-500 opacity-20 blur-3xl"
        animate={{ y: [0, -20, 0], x: [0, 10, 0] }}
        transition={{ duration: 12, repeat: Infinity }}
      />
      <motion.div
        aria-hidden
        className="pointer-events-none fixed right-0 bottom-0 w-72 h-72 rounded-full bg-gradient-to-br from-cyan-400 to-purple-500 opacity-20 blur-3xl"
        animate={{ y: [0, 15, 0], x: [0, -10, 0] }}
        transition={{ duration: 10, repeat: Infinity }}
      />

      {/* NAVIGATION */}
      <header className="fixed w-full top-4 z-40">
        <nav className="max-w-7xl mx-auto px-6 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <div className="w-10 h-10 rounded-lg bg-gradient-to-br from-indigo-500 to-pink-500 flex items-center justify-center text-black font-bold">
              AN
            </div>
            <div>
              <div className="font-semibold text-lg">AlgoNova</div>
              <div className="text-xs text-slate-400">
                Smarter Transactions. Intelligent Insights.
              </div>
            </div>
          </div>
          <div className="hidden md:flex items-center gap-6 text-sm">
            <button onClick={() => scrollTo("home")} className="hover:text-pink-400">Home</button>
            <button onClick={() => scrollTo("demo")} className="hover:text-pink-400">Demo</button>
            <button onClick={() => scrollTo("features")} className="hover:text-pink-400">Features</button>
            <button onClick={() => scrollTo("pricing")} className="hover:text-pink-400">Pricing</button>
            <button onClick={() => scrollTo("contact")} className="hover:text-pink-400">Contact</button>
            <a href="#cta" className="px-4 py-2 rounded-lg bg-gradient-to-r from-indigo-600 to-pink-500 text-black font-semibold">
              Get Started
            </a>
          </div>
        </nav>
      </header>

      {/* HERO */}
      <section id="home" className="min-h-screen flex flex-col justify-center items-center text-center px-6">
        <motion.h1
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 1 }}
          className="text-5xl md:text-7xl font-extrabold leading-tight"
        >
          <span className="bg-clip-text text-transparent bg-gradient-to-r from-indigo-400 via-pink-500 to-purple-400">
            Welcome to AlgoNova
          </span>
        </motion.h1>

        <motion.p
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.3, duration: 0.8 }}
          className="mt-6 text-lg text-slate-300 max-w-2xl"
        >
          The world’s first AI-driven wallet that turns your data into decisions.
          Experience intelligent transactions, automated analytics, and seamless
          cross-chain management — all in one futuristic app.
        </motion.p>

        <div className="mt-8 flex gap-4">
          <button
            onClick={() => scrollTo("demo")}
            className="px-6 py-3 rounded-xl bg-gradient-to-r from-indigo-500 to-pink-500 text-black font-semibold"
          >
            Watch Demo
          </button>
          <button
            onClick={() => scrollTo("pricing")}
            className="px-6 py-3 rounded-xl border border-slate-700 text-slate-300 hover:text-white"
          >
            Get Started
          </button>
        </div>
      </section>

      {/* LIVE DEMO SECTION */}
      <motion.section
        id="demo"
        initial={{ opacity: 0, y: 60 }}
        whileInView={{ opacity: 1, y: 0 }}
        transition={{ duration: 1 }}
        viewport={{ once: true }}
        className="py-28 flex flex-col items-center justify-center bg-gradient-to-b from-transparent via-indigo-950/40 to-black/80 px-6"
      >
        <SectionTitle>See AlgoNova in Action</SectionTitle>

        <div className="relative w-full max-w-5xl rounded-3xl overflow-hidden backdrop-blur-lg bg-white/5 border border-white/10 shadow-[0_0_40px_rgba(255,255,255,0.1)]">
          <video
            src="/algonova-demo.mp4"
            autoPlay
            muted
            loop
            playsInline
            controls
            className="w-full h-full object-cover"
          />
        </div>

        <p className="mt-10 text-center text-slate-300 text-lg max-w-2xl">
          Watch how <span className="text-pink-400 font-semibold">AlgoNova</span> brings AI-driven intelligence to your transactions — visualize spending, automate decisions, and unlock insights instantly.
        </p>
      </motion.section>

      {/* FEATURES */}
      <section id="features" className="py-20 px-6">
        <SectionTitle>Powerful Features</SectionTitle>
        <div className="max-w-6xl mx-auto grid grid-cols-1 md:grid-cols-3 gap-6">
          {[
            ["⚡", "Instant Transfers", "Send & receive funds globally with minimal fees and max speed."],
            ["🤖", "AI Insights", "Smart categorization, predictions, and tailored financial suggestions."],
            ["🌐", "Cross-Chain Wallet", "Manage BTC, ETH, and stablecoins seamlessly in one dashboard."],
          ].map(([icon, title, text], i) => (
            <motion.div
              key={i}
              whileHover={{ scale: 1.03 }}
              className="p-6 rounded-2xl backdrop-blur-lg bg-white/5 border border-white/10 shadow hover:shadow-[0_0_30px_rgba(255,255,255,0.1)]"
            >
              <div className="text-3xl mb-4">{icon}</div>
              <h3 className="text-xl font-semibold mb-2">{title}</h3>
              <p className="text-slate-300">{text}</p>
            </motion.div>
          ))}
        </div>
      </section>

      {/* PRICING */}
      <section id="pricing" className="py-20 px-6 bg-black/60">
        <SectionTitle>Pricing Plans</SectionTitle>
        <div className="max-w-6xl mx-auto grid md:grid-cols-3 gap-6">
          {[
            ["Starter", "Free", ["Basic wallet", "Standard analytics"]],
            ["Pro", "$9.99/mo", ["AI insights", "Priority support"]],
            ["Enterprise", "Custom", ["Dedicated AI", "Team dashboards"]],
          ].map(([plan, price, features], i) => (
            <motion.div
              key={i}
              whileHover={{ scale: 1.02 }}
              className={`p-6 rounded-2xl border ${
                i === 1
                  ? "bg-gradient-to-br from-indigo-600 to-pink-500 text-black shadow-lg"
                  : "bg-white/5 border-white/10"
              }`}
            >
              <h3 className="text-xl font-semibold">{plan}</h3>
              <p className="mt-2 text-3xl font-bold">{price}</p>
              <ul className="mt-4 space-y-2 text-sm">
                {features.map((f, j) => (
                  <li key={j}>• {f}</li>
                ))}
              </ul>
              <button className={`mt-6 w-full py-2 rounded-lg ${i === 1 ? "bg-black text-white" : "bg-gradient-to-r from-indigo-500 to-pink-500 text-black font-semibold"}`}>
                {i === 2 ? "Contact Us" : "Get Started"}
              </button>
            </motion.div>
          ))}
        </div>
      </section>

      {/* CONTACT */}
      <section id="contact" className="py-20 px-6">
        <SectionTitle>Contact Us</SectionTitle>
        <form
          action="https://formspree.io/f/yourformid"
          method="POST"
          className="max-w-3xl mx-auto grid gap-4"
        >
          <input
            name="name"
            placeholder="Your Name"
            required
            className="p-3 rounded-lg bg-gray-900 border border-slate-700"
          />
          <input
            name="email"
            type="email"
            placeholder="Your Email"
            required
            className="p-3 rounded-lg bg-gray-900 border border-slate-700"
          />
          <textarea
            name="message"
            rows="5"
            placeholder="Your Message"
            required
            className="p-3 rounded-lg bg-gray-900 border border-slate-700"
          />
          <button
            type="submit"
            className="px-5 py-3 rounded-lg bg-gradient-to-r from-indigo-500 to-pink-500 text-black font-semibold"
          >
            Send Message
          </button>
        </form>
      </section>

      {/* FOOTER */}
      <footer className="py-8 text-center text-slate-500 border-t border-slate-800">
        © {new Date().getFullYear()} AlgoNova — Smarter Transactions. Intelligent Insights.
      </footer>
    </div>
  );
}
